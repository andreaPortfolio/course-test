import styled from "styled-components";
import { defaultStyledProps, DefaultStyledProps } from "../../helper";

const Box = styled.div<DefaultStyledProps>`
  ${defaultStyledProps};
`;

export default Box;
